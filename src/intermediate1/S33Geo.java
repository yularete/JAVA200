package intermediate1;

public class S33Geo {
    public double latitude;
    public double longitude;

    //객체의 멤버는 해당 객체에서 가장 중요한 데이터이다. 멤벌르 보면 어떤 객체인지 짐작할 수 있을 정도로.
    // 위도와 경도를 갖는 객체는 "위치를 표현하려고 한다." 라는 성격(Character)이 보인다. 그래서 멤버를 객체의 속성(Attribute)라고도 한다.
    //객체로 사용하려면 객체의 설계도인 클래스를 만들어야 한다. 클래스는 다음과 같은 형식을 취한다.

    // public class 클래스 이름{ 타입 멤버;}

    // 클래스이름 레퍼런스  = new 클래스이름(); 형식으로 객체를 생성한다.
}
